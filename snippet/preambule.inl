#ifndef JCPPY_PREAMBULE
#define JCPPY_PREAMBULE


#define JCPPY_EPSILON 0.00001
#define JCPPY_MIN(x, y) ((x) < (y) ? (x) : (y))
#define JCPPY_MAX(x, y) ((x) > (y) ? (x) : (y))

#endif // JCPPY_PREAMBULE
